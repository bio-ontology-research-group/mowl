[build-system]
requires = [
    "setuptools>=82.0.1",
    "wheel",
    "setuptools_scm[toml]>=10.0.5"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
local_scheme = "no-local-version"
